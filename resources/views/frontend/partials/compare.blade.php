<a href="{{ route('flash-deals') }}"
    class="d-none d-lg-flex size-45px bg-light align-items-center justify-content-center rounded-10 rounded-circle">
    <i class="las la-rocket la-2x opacity-80"></i>
    {{-- <span class="flex-grow-1 ml-1">
        @if (Session::has('compare'))
            <span class="badge bg-light badge-inline badge-pill">{{ count(Session::get('compare')) }}</span>
        @else
            <span class="badge bg-light badge-inline badge-pill">0</span>
        @endif
        <span class="nav-box-text d-none d-xl-block opacity-70">{{ translate('Compare') }}</span>
    </span> --}}
</a>
